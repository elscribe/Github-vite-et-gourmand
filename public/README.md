# Dossier Public

Le dossier `public/` est le seul repertoire qui doit etre expose par le serveur
web.

Il contient le controleur frontal `index.php` et les assets publics. Les classes
de l'application restent dans `app/` afin de ne pas etre accessibles directement
depuis le navigateur.
