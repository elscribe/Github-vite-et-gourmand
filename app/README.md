# Dossier App

Le dossier `app/` contient le code source PHP prive de l'application.

Il est volontairement place en dehors de `public/` : les utilisateurs peuvent
atteindre uniquement le controleur frontal et les assets, pas les classes PHP
directement.
